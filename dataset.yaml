train: ../datasets/custom_dataset/train/images  # Update this path
val: ../datasets/custom_dataset/val/images      # Update this path
#test: ../datasets/custom_dataset/test/images     # Optional, update this path if used

nc: 1  # Update this to the number of classes you have (1 if only 'signature')
names: ['signature']  # Replace 'signature' with your actual class name
